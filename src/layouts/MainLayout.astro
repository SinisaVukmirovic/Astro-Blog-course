---
// interface Props {
//     title: string;
//     description: string;
// }


// to make interface Props optional
// interface Props {
    //     title?: string;
    //     description?: string;
    // }

import MainHead from './MainHead.astro';
import Footer from '../components/Footer.astro';
import Navbar from '../components/Navbar.astro';

// importing global styles
import '../styles/global.css';

// const { title, description } = Astro.props;
// if props are optional they can have default values
const { title = 'My Astro Blog About page', description = 'Astro Blog tutorial with CiP ' } = Astro.props;

---

<html lang="en">
    <!-- <MainHead title={title} description={description}> -->
    <MainHead {title} {description}>

    </MainHead>

	<body>
        <Navbar />

		<main>
            <slot>
                Default text
            </slot>
        </main>

        <Footer />
	</body>
</html>